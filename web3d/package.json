{
  "name": "pythinfilm-web3d",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "sync-registry": "node scripts/sync-registry.mjs",
    "predev": "npm run sync-registry",
    "dev": "vite",
    "prebuild": "npm run sync-registry",
    "build": "vite build",
    "preview": "vite preview",
    "pretest": "npm run sync-registry",
    "test": "vitest run"
  },
  "dependencies": {
    "three": "^0.170.0"
  },
  "devDependencies": {
    "jsdom": "^30.0.0",
    "vite": "^5.4.10",
    "vitest": "^2.1.4"
  }
}
